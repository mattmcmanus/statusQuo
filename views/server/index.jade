- if (servers.production.length)
  h2.section Production
  !=partial('_server', servers.production)

- if (servers.development.length)
  h2.section Development
  !=partial('_server', servers.development)
  
- if (servers.other.length)
  h2.section Other
  !=partial('_server', servers.other)

- if (servers.length == 0)  
  img.psst(src="/images/psst.png")