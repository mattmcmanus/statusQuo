.server(id=server.id)
  header
    h2= server.name
    .buttons
      a.refresh(href="javascript:void(0)") Refresh
  
  section.info
    ul
      li= server.ip
      li= server.os
  section.services
    .loader
    ul